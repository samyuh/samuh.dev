# samuh.dev

Personal portfolio and blog.

## Install and Run
### Prerequisites

- Node.js 18+ (LTS recommended)
- npm (comes with Node.js)

### Commands

Run these in order:

```bash
npm install
npm run dev
```

After `npm run dev`, open the URL shown in `http://localhost:4321`.

